# Shared ESLint config

`create-config.js` contains workspace-wide TypeScript, formatting, naming, and import defaults. App-specific architecture boundaries stay in each app's `eslint.config.mjs`, where paths and module ownership are known.
