# Library

Shared technical helpers belong here only after they have two real consumers. This folder must not import module internals.

Do not create generic `constants`, `utils`, or `types` subfolders until real shared code requires them. Prefer purpose-specific filenames and inferred types.
