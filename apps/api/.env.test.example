DATABASE_URL=postgresql://root:root@localhost:5432/fastify_vue_test
JWT_SECRET=test-secret-at-least-32-characters-long
PORT=3000
HOST=0.0.0.0
NODE_ENV=test
LOG_LEVEL=silent
# CORS_ORIGIN=http://localhost:5173 # Only needed for separate API/web origins.
# REDIS_URL=redis://localhost:6379 # Not required in test; rate-limit falls back to an in-memory store.
