DATABASE_URL=postgresql://root:root@localhost:5432/fastify_vue
JWT_SECRET=replace-with-at-least-32-random-characters
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
# CORS_ORIGIN=http://localhost:5173 # Only needed for separate API/web origins.
# REDIS_URL=redis://localhost:6379 # Required in production for the shared rate-limit store; optional (in-memory) elsewhere.
