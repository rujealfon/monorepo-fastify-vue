# No API URL env var needed. apps/web/vercel.json rewrites /api/* to the API
# project (matching vite.config.ts's dev server proxy), so requests are always
# same-origin. See README.md's "Web project" section.
